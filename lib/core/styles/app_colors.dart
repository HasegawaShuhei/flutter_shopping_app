import 'package:flutter/material.dart';

// default colors [https://aakira.app/blog/2021/09/flutter-default-colors/]
class AppColors {
  // theme(shared)
  static const primaryColor = Color(0xFF00BFA5);
  // black
  static const black = Color(0xFF000000);
  static const lightBlack = Color(0xDD000000);
  static const transparentBlack = Color(0x42000000);
  // white
  static const white = Color(0xFFFFFFFF);
  // grey
  static const darkGrey = Color(0xFF212121);
  static const lightGrey = Color(0xFFE0E0E0);
  // yellow
  static const amber = Color(0xFFFFC107);
  // others
  static const transparent = Colors.transparent;
}

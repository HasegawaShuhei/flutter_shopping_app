import 'package:flutter/material.dart';

// default colors [https://aakira.app/blog/2021/09/flutter-default-colors/]
class AppColors {
  // theme(shared)
  // static const accentColor = Color(0xFF64FFDA);
  static const primaryColor = Color(0xFF00BFA5);
  // theme(light)
  static const lightPrimaryColor = Color(0xFF2196F3);
  static const lightBackgroundColor = Color(0xFFFAFAFA);
  // theme(dark)
  static const darkPrimaryColor = Color(0xFF212121);
  static const darkBackgroundColor = Color(0xFF303030);
  // black
  static const black = Colors.black;
  // white
  static const white = Colors.white;
}
